<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.court.supporter.notice.noticeMapper">

	<insert id="noticeRegist" parameterType="noticeVO">
		INSERT INTO TB_003 (NOTICE_TITLE,
							NOTICE_CONTENT,
							NOTICE_DATE,
							ADMIN_PROPER_NUM)
		VALUES (#{NOTICE_TITLE},
				#{NOTICE_CONTENT},
				NOW(),
				#{ADMIN_PROPER_NUM})
		
	</insert>
	
	<select id="noticeList" resultType="noticeVO">
		select * from TB_003
		
		where 1=1
		
		<if test="cri.searchNotice !='' and cri.searchNotice != null">
		and NOTICE_TITLE like concat('%',#{cri.searchNotice},'%') 
		</if>
		
		order by
		notice_proper_num desc
		limit #{cri.pageStart}, #{cri.amount}
		
	</select>
	
	<select id="getTotal" resultType="int">
		select count(*) as total from TB_003
		
	</select>



</mapper>