const swiper = new Swiper('.swiper', {
    // Optional parameters
    direction: 'horizontal',
    autoplay:{
      delay:5000,
      disableOninteraction:false
    },
    loop: true,
    loopAdditionalSlides:1
  });